{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npx prisma generate"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && npx prisma db push --accept-data-loss && node prisma/init-railway.js && node src/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}