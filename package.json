{
  "name": "sabonesa-dental-backend",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate",
    "start": "cd backend && node src/index.js",
    "db:push": "cd backend && npx prisma db push",
    "db:seed": "cd backend && node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "pino": "^10.3.1",
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0"
  }
}
